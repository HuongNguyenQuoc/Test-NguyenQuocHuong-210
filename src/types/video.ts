export type VideoItem = {
  id: string;
  videoUrl: string;
  authorName: string;
  description: string;
  likesCount: number;
  commentsCount: number;
  sharesCount: number;
};

// This defines the TypeScript type for one video item.
// This helps TypeScript check your data and props.
